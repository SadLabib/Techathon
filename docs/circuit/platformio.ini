; PlatformIO project so the Wokwi VS Code extension has firmware to simulate.
; Build with the PlatformIO sidebar (checkmark icon) or `pio run` from this folder.
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
